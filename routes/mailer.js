const express = require('express');
const router = express.Router();

// Use this route to handle updating the database and then redirect user back to login page
 
router
    .route('/')
    // Retrieve user id from URL
    // SQL here
    // Redirect to login page once email address is updated

module.exports = router